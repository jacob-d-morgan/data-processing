function [aliquot_deltas_csCorr] = chemSlopeCorr(aliquot_deltas,aliquot_metadata)
% CSCORR corrects all the delta values in ALIQUOT_DELTAS for the effect of
% the chemical slope. The correction is determined using the delta values
% from the CS experiment aliquots and their metadata in ALIQUOT_DELTA and
% ALIQUOT_METADATA.
%
%
%  ------------------------------------------------------------------------

end